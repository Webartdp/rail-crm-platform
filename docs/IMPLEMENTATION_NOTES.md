# Implementation Notes

## Assignment naming

Product documents use the word `assignment` for planned work.

In the first database draft, the migration table is named `work_orders` to keep the implementation clear and simple.

Later we can decide whether the final backend name should be:

- assignments
- work_orders
- deployments

## Current visible result

Frontend prototype:

```text
http://localhost:3000
```

Interactive button flow demo:

```text
http://localhost:3000/demo
```

## Current backend base

Added migration drafts for:

- employee_profiles
- work_events
- work_sessions
- work_orders
